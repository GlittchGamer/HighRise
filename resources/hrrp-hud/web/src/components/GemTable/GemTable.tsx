import { Fade } from '@mui/material';

import './GemTable.scss';

const GemTable = () => {
  return null;
  // const dispatch = useDispatch();
  // const showing = useSelector((state: RootState) => state.gemTable.showing);
  // const info = useSelector((state: RootState) => state.gemTable.info);

  // return (
  //   <Fade dir="down" in={showing} timeout={500} mountOnEnter unmountOnExit>
  //     <div className={"gemtable_wrapper"}>
  //       <div className={`blackBar top`}>SAGMA Gem Table</div>
  //       <div className={`blackBar bottom`}>Appraised Gem Quality: {info}%</div>
  //     </div>
  //   </Fade>
  // );
};

export default GemTable;
